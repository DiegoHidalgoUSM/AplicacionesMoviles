import 'package:flutter/material.dart';
class TabHoteles extends StatelessWidget {
  const TabHoteles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hoteles'),
      ),
    );
  }
}